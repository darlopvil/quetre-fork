//-//////////////////////////////////////////////////////
//-                 INCLUDES/EXTENDS
//-//////////////////////////////////////////////////////
include _formatText
include _metadata
include _utils

//-//////////////////////////////////////////////////////
//-                 MAIN CONTENT
//-//////////////////////////////////////////////////////
mixin addQuestion(question)
  article.answer
    h3.answer__question.heading.heading__tertiary
      a.answer__link.answers__link(href=question.url)
        +spansChecker(question.text[0].spans)

    .metadata-secondary
      +addMetadataSecondary('clock', 'Preguntada', question.creationTime, 'date')
      if question.lastFollowTime
        +addMetadataSecondary('clock-edit', 'Último seguidor', question.lastFollowTime, 'date')
      +addMetadataSecondary('users', 'seguidores', question.numFollowers)
      if question.numAnswers
        +addMetadataSecondary('answers', 'Respuestas', question.numAnswers)
      +addMetadataSecondary('comments', 'Comentarios', question.numComments)